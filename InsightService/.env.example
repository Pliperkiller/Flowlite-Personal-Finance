# ===========================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ===========================================
# IMPORTANTE:
# 1. Copiar este archivo como .env
# 2. Reemplazar los valores con tus credenciales reales
# 3. NUNCA commitear el archivo .env al repositorio
# 4. La infraestructura (MySQL y RabbitMQ) debe estar corriendo
#    desde InfrastructureService antes de ejecutar este servicio
# ===========================================

# Database (MySQL)
# NOTA: La base de datos debe estar inicializada desde InfrastructureService
# Para desarrollo local: mysql+pymysql://flowlite_user:flowlite_password@localhost:3306/flowlite_db
DATABASE_URL=mysql+pymysql://flowlite_user:flowlite_password@localhost:3306/flowlite_db

# RabbitMQ
# NOTA: RabbitMQ debe estar corriendo desde InfrastructureService
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=admin
RABBITMQ_QUEUE=batch_processed
RABBITMQ_PREFETCH_COUNT=1

# LLM (Ollama) - Servidor Remoto
# -----------------------------------------
# Conexión Directa (solo redes privadas):
#   OLLAMA_HOST=http://IP_SERVIDOR:11434
# -----------------------------------------
OLLAMA_HOST=http://localhost:11434
LLM_MODEL=llama3.1:8b
LLM_TEMPERATURE=0.7
LLM_TIMEOUT=120

# Logging
LOG_LEVEL=INFO

# API HTTP (Health Check y monitoreo)
API_HOST=0.0.0.0
API_PORT=8002
