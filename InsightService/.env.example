# ===========================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ===========================================
# IMPORTANTE:
# 1. Copiar este archivo como .env
# 2. Reemplazar los valores con tus credenciales reales
# 3. NUNCA commitear el archivo .env al repositorio
# ===========================================

# Database (PostgreSQL)
# Para desarrollo local con Docker: postgresql://postgres:postgres@localhost:5432/insights_db
DATABASE_URL=postgresql://usuario:contraseña@localhost:5432/nombre_bd

# RabbitMQ
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=tu_usuario
RABBITMQ_PASSWORD=tu_contraseña
RABBITMQ_QUEUE=transaction_processed
RABBITMQ_PREFETCH_COUNT=1

# LLM (Ollama) - Servidor Remoto
# -----------------------------------------
# Opción 1 - Túnel SSH (Recomendado):
#   1. Configurar variables en terminal:
#      export OLLAMA_REMOTE_USER=tu_usuario_ssh
#      export OLLAMA_REMOTE_HOST=ip_del_servidor
#   2. Ejecutar: ./scripts/start_ollama_tunnel.sh
#   3. Usar: OLLAMA_HOST=http://localhost:11434
#
# Opción 2 - Conexión Directa (solo redes privadas):
#   OLLAMA_HOST=http://IP_SERVIDOR:11434
# -----------------------------------------
OLLAMA_HOST=http://localhost:11434
LLM_MODEL=llama3.1:8b
LLM_TEMPERATURE=0.7
LLM_TIMEOUT=120

# Logging
LOG_LEVEL=INFO

# ===========================================
# Variables opcionales para Docker Compose
# ===========================================
# Si usas docker-compose, puedes personalizar:
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=insights_db
POSTGRES_PORT=5432

RABBITMQ_MGMT_PORT=15672
